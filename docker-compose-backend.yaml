services:
  backend:
    build:
      context: ./backend/
      dockerfile: Dockerfile
    container_name: meowsic-backend
    restart: unless-stopped
    ports:
      - "3457:3457"
    volumes:
      - meowsic-data:/data
      - ./music:/music:ro

volumes:
  meowsic-data:
